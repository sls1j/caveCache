function CaveMediaAddViewModel(nav, agent)
{
        ViewModel(this, "cave-media-add", nav, agent);
    
        var public = this;
        var private = public.private;
        var protected = public.protected;
    
}